apiVersion: v1
kind: Service
metadata:
  name: {{ include "pull-request-preview.fullname" . }}
  labels:
    {{- include "pull-request-preview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pull-request-preview.selectorLabels" . | nindent 4 }}
