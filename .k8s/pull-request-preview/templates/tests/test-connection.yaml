apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pull-request-preview.fullname" . }}-test-connection"
  labels:
    {{- include "pull-request-preview.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pull-request-preview.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
